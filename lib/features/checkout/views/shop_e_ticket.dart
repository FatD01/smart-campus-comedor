import 'package:flutter/material.dart';

class ShopETicket extends StatelessWidget {
   
  const ShopETicket({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
         child: Text('ShopETicket'),
      ),
    );
  }
}