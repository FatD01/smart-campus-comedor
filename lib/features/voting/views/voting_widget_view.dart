import 'package:flutter/material.dart';

class VotingWidget extends StatelessWidget {
   
  const VotingWidget({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
         child: Text('Votingwidget'),
      ),
    );
  }
}