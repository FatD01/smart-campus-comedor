import 'package:flutter/material.dart';

class TicketConfirmationScreen extends StatelessWidget {
   
  const TicketConfirmationScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
         child: Text('TicketConfirmationScreen'),
      ),
    );
  }
}